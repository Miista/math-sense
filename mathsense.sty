\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{mathsense}[2016/11/02 MathSense package]

\RequirePackage{amssymb} % Contains many math symbols
\RequirePackage{amsfonts} % Provide fonts for Blackboard Bold

% Internal {{{
\newcommand{\automath}[1]{%
  % if in math mode, print as normal
  % otherwise, wrap the argument in $$
  \ifmmode#1\else$#1$\fi%
}
% }}}

% Wraps the arguments in angle brackets.
\newcommand{\tuple}[1]{\automath{\langle#1\rangle}}

\newcommand{\size}[1]{\left|\; #1 \;\right|}
\let\pair\tuple

\let\phi\varphi                         % Proper phi
\let\emptyset\varnothing                % Prettier empty set sign

% If the package 'amsthm' is loaded, we change the QED symbol
\IfFileExists{amsthm.sty}
  { \RequirePackage{amsthm}
    \renewcommand\qedsymbol{$\blacksquare$} % Set new QED symbol
  }

\let\Union\bigcup       % Big union
\let\union\cup          % Small union
\let\intersects\cap     % Small intersection
\let\Intersects\bigcap  % Big intersections
\let\OR\vee             % Or \/
\let\AND\wedge          % And /\
\let\without\setminus   % Set removal


% Number Classes {{{
\newcommand{\N}{%   Natural numbers
  \automath{ \mathbb{N} }
}

\newcommand{\R}{%   Real numbers
  \automath{ \mathbb{R} }
}

\newcommand{\Q}{%   Rational numbers
  \automath{ \mathbb{Q} }
}

\newcommand{\Z}{%   Integers
  \automath{ \mathbb{Z} }
}

% We use renew here because \C might have been defined
% if using XeLaTeX or by loading hyperref:
% https://tex.stackexchange.com/a/2968
\renewcommand{\C}{%   Complex numbers
 \automath{ \mathbb{C} }
}
% }}}

% vim: foldmethod=marker
